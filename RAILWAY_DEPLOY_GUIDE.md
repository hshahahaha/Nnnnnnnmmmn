# 🚂 دليل رفع البوت على Railway

## 📋 الخطوات:

### 1. تحضير الملفات

تأكد من وجود هذه الملفات في المشروع:
- ✅ `telegram_checker_bot_proxy.py` - الكود الرئيسي
- ✅ `requirements.txt` - المكتبات المطلوبة
- ✅ `Procfile` - أوامر التشغيل
- ✅ `railway.json` - إعدادات Railway
- ✅ `runtime.txt` - نسخة Python
- ✅ `.gitignore` - الملفات المستبعدة

---

### 2. رفع الملفات على Railway

#### الطريقة 1: عبر GitHub (الأفضل)

1. **أنشئ مستودع GitHub جديد**
   - اذهب إلى https://github.com/new
   - أنشئ مستودع خاص (Private)

2. **ارفع الملفات للمستودع**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git branch -M main
   git remote add origin <رابط-المستودع>
   git push -u origin main
   ```

3. **اربط Railway بـ GitHub**
   - اذهب إلى Railway Dashboard
   - اضغط "New Project"
   - اختر "Deploy from GitHub repo"
   - اختر المستودع الذي أنشأته

#### الطريقة 2: عبر Railway CLI

1. **ثبّت Railway CLI**
   ```bash
   npm i -g @railway/cli
   ```

2. **سجل الدخول**
   ```bash
   railway login
   ```

3. **اربط المشروع**
   ```bash
   railway link
   ```
   اختر المشروع الموجود: `895f4638-18aa-4572-ac9b-bd373f4d590a`

4. **ارفع الملفات**
   ```bash
   railway up
   ```

#### الطريقة 3: رفع يدوي

1. اذهب إلى Railway Dashboard
2. اضغط على المشروع
3. اضغط "Settings" → "Source"
4. ارفع الملفات يدوياً

---

### 3. إعداد المتغيرات البيئية (Environment Variables)

في Railway Dashboard:

1. اذهب إلى "Variables"
2. أضف المتغيرات التالية:

```
BOT_TOKEN=8207958853:AAGx9Te8ZBMI8h-z7RrWqVwWz3dxq_BPCbM
AUTHORIZED_USER_ID=1427023555
```

**مهم:** لا تشارك هذه المعلومات مع أحد!

---

### 4. تشغيل البوت

بعد رفع الملفات وإضافة المتغيرات:

1. Railway سيبني المشروع تلقائياً
2. سيشغل البوت باستخدام الأمر في `Procfile`
3. تحقق من السجلات (Logs) للتأكد من التشغيل

---

### 5. التحقق من التشغيل

#### في Railway Dashboard:

1. اذهب إلى "Deployments"
2. تحقق من حالة آخر Deployment
3. يجب أن يكون "Success" ✅

#### في السجلات (Logs):

يجب أن ترى:
```
🤖 Starting Telegram Card Checker Bot with Proxy Support...
🔐 Authorized User ID: 1427023555
🔑 Bot Token: 8207958853:AAGx9Te...
✅ Bot started successfully!
🔗 Open Telegram and send /start to your bot
```

#### في تلجرام:

1. افتح البوت
2. أرسل `/start`
3. يجب أن يرد عليك!

---

## 🔧 إصلاح المشاكل الشائعة

### المشكلة 1: "Script start.sh not found"

**الحل:**
- تأكد من وجود ملف `Procfile` أو `railway.json`
- تأكد من أن الأمر صحيح: `python telegram_checker_bot_proxy.py`

### المشكلة 2: "Module not found"

**الحل:**
- تأكد من وجود `requirements.txt`
- تأكد من أن جميع المكتبات مذكورة فيه

### المشكلة 3: البوت لا يرد

**الحل:**
- تحقق من المتغيرات البيئية (BOT_TOKEN, AUTHORIZED_USER_ID)
- تحقق من السجلات للأخطاء
- تأكد من أن البوت يعمل (Status: Running)

### المشكلة 4: "Application failed to respond"

**الحل:**
- Railway يتوقع تطبيق ويب، لكن هذا بوت تلجرام
- تأكد من أن نوع الخدمة "Worker" وليس "Web"
- في `railway.json` تأكد من `"builder": "NIXPACKS"`

---

## 📊 مراقبة البوت

### السجلات (Logs):

```bash
# عبر Railway CLI
railway logs
```

أو من Dashboard → Deployments → View Logs

### إعادة التشغيل:

```bash
# عبر Railway CLI
railway restart
```

أو من Dashboard → Settings → Restart

---

## 🔄 تحديث البوت

### عبر GitHub:

1. عدّل الملفات
2. ارفع التحديثات:
   ```bash
   git add .
   git commit -m "Update bot"
   git push
   ```
3. Railway سيعيد البناء تلقائياً

### عبر Railway CLI:

```bash
railway up
```

---

## 💾 النسخ الاحتياطي

### حفظ البروكسيات:

البروكسيات محفوظة في `proxies.txt` على Railway.

**لتحميلها:**
```bash
railway run cat proxies.txt
```

**لرفع بروكسيات جديدة:**
1. عدّل `proxies.txt` محلياً
2. ارفع التحديثات على GitHub/Railway

---

## ⚠️ ملاحظات مهمة

1. **Railway المجاني:**
   - 500 ساعة شهرياً (كافية لبوت واحد)
   - إذا انتهت الساعات، البوت سيتوقف

2. **الأمان:**
   - لا تشارك `BOT_TOKEN` مع أحد
   - لا تشارك رابط المشروع على Railway
   - احتفظ بالمتغيرات البيئية سرية

3. **الأداء:**
   - Railway قد يكون أبطأ من السيرفر الخاص
   - استخدم البروكسيات لتحسين الأداء

---

## 📞 الدعم

إذا واجهت مشاكل:

1. تحقق من السجلات أولاً
2. تأكد من جميع الملفات موجودة
3. تأكد من المتغيرات البيئية صحيحة
4. جرب إعادة التشغيل

---

## ✅ قائمة التحقق النهائية

قبل الرفع، تأكد من:

- [ ] جميع الملفات موجودة
- [ ] `requirements.txt` يحتوي على جميع المكتبات
- [ ] `Procfile` يحتوي على الأمر الصحيح
- [ ] المتغيرات البيئية مضافة في Railway
- [ ] البوت يعمل محلياً بدون أخطاء

---

**بالتوفيق!** 🚀

إذا اتبعت هذه الخطوات، البوت سيعمل على Railway بنجاح!
